# docker-compose overrides — defaults work out of the box for local development
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=starter
# Host port for Postgres (5433 avoids clashing with a locally running Postgres)
POSTGRES_PORT=5433
