{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "lint": {},
    "test": { "dependsOn": ["build"] },
    "test:e2e": {
      "dependsOn": ["build"],
      "cache": false,
      "env": [
        "DATABASE_URL",
        "REDIS_URL",
        "REDIS_IS_CLUSTER",
        "AWS_REGION",
        "AWS_ENDPOINT_URL",
        "AWS_ACCESS_KEY_ID",
        "AWS_SECRET_ACCESS_KEY",
        "S3_ENABLED",
        "S3_ENDPOINT",
        "S3_BUCKET_NAME",
        "S3_ACCESS_KEY",
        "S3_SECRET_KEY",
        "SQS_ENABLED",
        "SNS_ENABLED",
        "MAIL_ENABLED",
        "MAIL_FROM_ADDRESS",
        "LAMBDA_ENABLED",
        "AUTH_JWT_SECRET",
        "AUTH_ACCESS_TTL_SEC",
        "AUTH_REFRESH_TTL_SEC",
        "AUTH_REFRESH_GRACE_SEC"
      ]
    }
  }
}
