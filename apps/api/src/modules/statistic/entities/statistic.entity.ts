// CASL subject class — the ability metadata target for statistics permissions.
// No fields: the dashboard is an aggregate view, not a record, so there is
// nothing to field-scope — READ is granted or denied for the module as a whole.
export class StatisticEntity {}
