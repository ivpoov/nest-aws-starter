# Removing `statistic`

Admin dashboard statistics (cached TypedSQL aggregates).

Generated by `scripts/subtraction-test.mjs --emit-docs` from the `// <module:statistic>`
fence markers in the codebase — do not hand-edit; re-run the generator instead.

## 1. Delete

- `apps/api/src/modules/statistic` (delete)
- `apps/api/prisma/sql` (delete)

## 2. Strip cross-module references

Every line/block below carries a `// <module:statistic>` (or
`// <module:statistic>` ... `// </module:statistic>`) fence comment. Delete
the marked lines/blocks and the markers themselves.

- `apps/api/src/app.module.ts`
  - line 21: `import { StatisticModule } from '@modules/statistic/statistic.module.js'; // <module:statistic>`
  - line 78: `StatisticModule, // <module:statistic>`

## 3. Drop `.env` variables

_No dedicated `.env` variables._

## 4. Verify

```
pnpm --dir apps/api exec tsc --noEmit -p tsconfig.build.json
pnpm --dir apps/api run test
```

This exact recipe is proven nightly by `scripts/subtraction-test.mjs --module statistic`.
