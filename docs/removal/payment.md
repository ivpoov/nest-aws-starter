# Removing `payment`

Plans, subscriptions, payment transactions, and webhook events (schema + core module).

Generated by `scripts/subtraction-test.mjs --emit-docs` from the `// <module:payment>`
fence markers in the codebase — do not hand-edit; re-run the generator instead.

## 1. Delete

- `apps/api/src/modules/payment` (delete)

## 2. Strip cross-module references

Every line/block below carries a `// <module:payment>` (or
`// <module:payment>` ... `// </module:payment>`) fence comment. Delete
the marked lines/blocks and the markers themselves.

- `apps/api/src/app.module.ts`
  - line 18: `import { PaymentModule } from '@modules/payment/payment.module.js'; // <module:payment>`
  - line 81: `PaymentModule, // <module:payment>`
- `apps/api/prisma/schema.prisma`
  - line 44: `subscriptions       Subscription[] // <module:payment>`
  - line 45: `paymentTransactions PaymentTransaction[] // <module:payment>`
  - lines 240-336 (block)

## 3. Drop `.env` variables

_No dedicated `.env` variables._

## 4. Verify

```
pnpm --dir apps/api exec tsc --noEmit -p tsconfig.build.json
pnpm --dir apps/api run test
```

This exact recipe is proven nightly by `scripts/subtraction-test.mjs --module payment`.
