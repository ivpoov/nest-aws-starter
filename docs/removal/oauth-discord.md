# Removing `oauth-discord`

Discord OAuth login/link provider.

Generated by `scripts/subtraction-test.mjs --emit-docs` from the `// <module:oauth-discord>`
fence markers in the codebase — do not hand-edit; re-run the generator instead.

## 1. Delete

- `apps/api/src/modules/oauth-discord` (delete)
- `apps/api/src/configs/discord-oauth.config.ts` (delete)

## 2. Strip cross-module references

Every line/block below carries a `// <module:oauth-discord>` (or
`// <module:oauth-discord>` ... `// </module:oauth-discord>`) fence comment. Delete
the marked lines/blocks and the markers themselves.

- `apps/api/src/app.module.ts`
  - line 15: `import { DiscordOauthModule } from '@modules/oauth-discord/discord-oauth.module.js'; // <module:oauth-discord>`
  - line 70: `DiscordOauthModule, // <module:oauth-discord>`
- `apps/api/src/configs/index.ts`
  - line 5: `import { discordOauthConfig } from '@configs/discord-oauth.config.js'; // <module:oauth-discord>`
  - line 22: `discordOauthConfig, // <module:oauth-discord>`

## 3. Drop `.env` variables

- `DISCORD_OAUTH_ENABLED`
- `DISCORD_OAUTH_CLIENT_ID`
- `DISCORD_OAUTH_CLIENT_SECRET`
- `DISCORD_OAUTH_REDIRECT_URI`

## 4. Verify

```
pnpm --dir apps/api exec tsc --noEmit -p tsconfig.build.json
pnpm --dir apps/api run test
```

This exact recipe is proven nightly by `scripts/subtraction-test.mjs --module oauth-discord`.
