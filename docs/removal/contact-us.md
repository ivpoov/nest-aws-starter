# Removing `contact-us`

Public contact form + admin inbox.

Generated by `scripts/subtraction-test.mjs --emit-docs` from the `// <module:contact-us>`
fence markers in the codebase — do not hand-edit; re-run the generator instead.

## 1. Delete

- `apps/api/src/modules/contact-us` (delete)

## 2. Strip cross-module references

Every line/block below carries a `// <module:contact-us>` (or
`// <module:contact-us>` ... `// </module:contact-us>`) fence comment. Delete
the marked lines/blocks and the markers themselves.

- `apps/api/src/app.module.ts`
  - line 9: `import { ContactUsModule } from '@modules/contact-us/contact-us.module.js'; // <module:contact-us>`
  - line 80: `ContactUsModule, // <module:contact-us>`
- `apps/api/prisma/schema.prisma`
  - lines 161-184 (block)

## 3. Drop `.env` variables

_No dedicated `.env` variables._

## 4. Verify

```
pnpm --dir apps/api exec tsc --noEmit -p tsconfig.build.json
pnpm --dir apps/api run test
```

This exact recipe is proven nightly by `scripts/subtraction-test.mjs --module contact-us`.
