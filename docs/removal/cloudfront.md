# Removing `cloudfront`

CloudFront signed download URLs (optional common provider).

Generated by `scripts/subtraction-test.mjs --emit-docs` from the `// <module:cloudfront>`
fence markers in the codebase — do not hand-edit; re-run the generator instead.

## 1. Delete

- `apps/api/src/modules/common/providers/cloudfront` (delete)
- `apps/api/src/configs/cloudfront.config.ts` (delete)

## 2. Strip cross-module references

Every line/block below carries a `// <module:cloudfront>` (or
`// <module:cloudfront>` ... `// </module:cloudfront>`) fence comment. Delete
the marked lines/blocks and the markers themselves.

- `apps/api/src/app.module.ts`
  - line 32: `import { CloudFrontModule } from '@providers/cloudfront/cloudfront.module.js'; // <module:cloudfront>`
  - line 60: `CloudFrontModule, // <module:cloudfront>`
- `apps/api/src/configs/index.ts`
  - line 3: `import { cloudfrontConfig } from '@configs/cloudfront.config.js'; // <module:cloudfront>`
  - line 23: `cloudfrontConfig, // <module:cloudfront>`
- `apps/api/src/modules/file/services/file.service.ts`
  - line 2: `import { type CloudFrontConfig, cloudfrontConfig } from '@configs/cloudfront.config.js'; // <module:cloudfront>`
  - line 34: `import { CLOUDFRONT_SIGNER } from '@providers/cloudfront/constants/cloudfront.constants.js'; // <module:cloudfront>`
  - line 35: `import type { CloudFrontSignerInterface } from '@providers/cloudfront/interfaces/cloudfront-signer.interface.js'; // <module:cloudfront>`
  - lines 49-54 (block)
  - lines 121-125 (block)
- `apps/api/src/modules/file/tests/file.service.spec.ts`
  - line 1: `import type { CloudFrontConfig } from '@configs/cloudfront.config.js'; // <module:cloudfront>`
  - line 10: `import type { CloudFrontSignerInterface } from '@providers/cloudfront/interfaces/cloudfront-signer.interface.js'; // <module:cloudfront>`
  - line 36: `readonly cloudFrontSigner: CloudFrontSignerInterface; // <module:cloudfront>`
  - line 43: `cloudFrontConfig: CloudFrontConfig = { isEnabled: false }, // <module:cloudfront>`
  - lines 59-63 (block)
  - lines 69-72 (block)
  - line 80: `cloudFrontSigner, // <module:cloudfront>`
  - line 211: `cloudFrontSigner, // <module:cloudfront>`
  - line 220: `expect(cloudFrontSigner.getSignedUrl).not.toHaveBeenCalled(); // <module:cloudfront>`
  - lines 223-243 (block)

## 3. Drop `.env` variables

- `CLOUDFRONT_ENABLED`
- `CLOUDFRONT_DOMAIN`
- `CLOUDFRONT_KEY_PAIR_ID`
- `CLOUDFRONT_PRIVATE_KEY`
- `CLOUDFRONT_URL_TTL_SEC`

## 4. Verify

```
pnpm --dir apps/api exec tsc --noEmit -p tsconfig.build.json
pnpm --dir apps/api run test
```

This exact recipe is proven nightly by `scripts/subtraction-test.mjs --module cloudfront`.
