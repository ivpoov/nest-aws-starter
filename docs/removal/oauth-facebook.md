# Removing `oauth-facebook`

Facebook OAuth login/link provider.

Generated by `scripts/subtraction-test.mjs --emit-docs` from the `// <module:oauth-facebook>`
fence markers in the codebase — do not hand-edit; re-run the generator instead.

## 1. Delete

- `apps/api/src/modules/oauth-facebook` (delete)
- `apps/api/src/configs/facebook-oauth.config.ts` (delete)

## 2. Strip cross-module references

Every line/block below carries a `// <module:oauth-facebook>` (or
`// <module:oauth-facebook>` ... `// </module:oauth-facebook>`) fence comment. Delete
the marked lines/blocks and the markers themselves.

- `apps/api/src/app.module.ts`
  - line 16: `import { FacebookOauthModule } from '@modules/oauth-facebook/facebook-oauth.module.js'; // <module:oauth-facebook>`
  - line 73: `FacebookOauthModule, // <module:oauth-facebook>`
- `apps/api/src/configs/index.ts`
  - line 6: `import { facebookOauthConfig } from '@configs/facebook-oauth.config.js'; // <module:oauth-facebook>`
  - line 25: `facebookOauthConfig, // <module:oauth-facebook>`

## 3. Drop `.env` variables

- `FACEBOOK_OAUTH_ENABLED`
- `FACEBOOK_OAUTH_CLIENT_ID`
- `FACEBOOK_OAUTH_CLIENT_SECRET`
- `FACEBOOK_OAUTH_REDIRECT_URI`

## 4. Verify

```
pnpm --dir apps/api exec tsc --noEmit -p tsconfig.build.json
pnpm --dir apps/api run test
```

This exact recipe is proven nightly by `scripts/subtraction-test.mjs --module oauth-facebook`.
